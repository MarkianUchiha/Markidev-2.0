---
import { getCollection } from 'astro:content';
const trabajos = await getCollection('trabajos');
console.log('Trabajos encontrados:', trabajos); // Para debug
---

<section class="py-20 bg-gradient-to-bl from-[#19274e] to-[#220114]">
  <div class="container mx-auto px-4">
    <h2 class="text-4xl font-bold text-white text-center">Mi Trabajo</h2>
    <p class="mt-4 text-lg text-gray-400 text-center max-w-3xl mx-auto mb-12">
      Aquí hay una selección de algunos de mis proyectos recientes. Cada uno
      representa un desafío único y una oportunidad para crecer.
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {trabajos.length > 0 ? (
        trabajos.map((trabajo) => (
          <div class="bg-gray-900/50 border border-white/50 rounded-lg overflow-hidden">
            <a href={`/trabajos/${trabajo.id}`}>
              <img
                src={trabajo.data.image}
                alt={trabajo.data.title}
                class="w-full h-48 object-cover"
              />
            </a>
            <div class="p-6">
              <h3 class="text-2xl font-bold text-white">{trabajo.data.title}</h3>
              <p class="text-gray-400 mt-2">{trabajo.data.description}</p>
              <div class="flex flex-wrap gap-2 mt-4">
                {trabajo.data.tags.map((tag) => (
                  <span class="bg-gray-700/60 text-gray-300 text-xs px-2 py-1 rounded-full">{tag}</span>
                ))}
              </div>
            </div>
          </div>
        ))
      ) : (
        <p class="text-white col-span-3 text-center">No se encontraron trabajos.</p>
      )}
    </div>
  </div>
</section>